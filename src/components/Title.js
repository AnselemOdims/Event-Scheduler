// import styled from 'styled-components';

const Title = ({title, subTitle}) => {
  return ( 
    <header>
      <h1>{title}</h1>
      <p>{subTitle}</p>
    </header>
   );
}
 
export default Title;